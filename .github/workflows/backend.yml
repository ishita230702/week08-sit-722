name: Backend Pipeline

on:
  pull_request:
    branches: [ "dev" ]
  push:
    branches: [ "workflow-update" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Simulate backend build
        run: |
          echo "Running backend build stage..."
          ls -R backend || echo "No backend folder contents"
